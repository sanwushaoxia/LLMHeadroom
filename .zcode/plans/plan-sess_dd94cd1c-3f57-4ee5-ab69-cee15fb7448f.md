No code changes are proposed for this question. Report the measured size difference, explain the JSON overhead (repeated field names and syntax, duplicated `raw` plus parsed fields, metadata, and the current Latin-1 decoding default), clarify that the default output is JSONL rather than one JSON document, and recommend `--encoding utf-8 --no-raw` when compact machine-readable output is preferred. Explain when JSON is useful versus when the existing TXT or gzip/zstd is the better storage format.